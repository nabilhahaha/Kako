import { PageStub } from "@/components/app/page-stub";
export default function Page() {
  return <PageStub title="Mapping Profiles" note="Per-agent versioned column & value mapping + calculation policy — screens #5/#6." />;
}
