import { PageStub } from "@/components/app/page-stub";

export default function Page() {
  return (
    <PageStub
      title="Workspace / Tasks"
      note="Task follow-up module (Monday-style): create/assign tasks with status, priority, due dates, and per-task visibility. Design proposed in docs/WORKSPACE-TASKS.md — screens land after the foundation is approved."
    />
  );
}
