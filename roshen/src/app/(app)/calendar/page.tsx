import { PageStub } from "@/components/app/page-stub";

export default function Page() {
  return (
    <PageStub
      title="Calendar"
      note="Month/Week/Day calendar of tasks, business trips, and leave (by due/start/end dates), scoped by visibility. Design in docs/WORKSPACE-TASKS.md & docs/REQUESTS-MODULE.md — built after foundation approval."
    />
  );
}
