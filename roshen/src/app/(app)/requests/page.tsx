import { PageStub } from "@/components/app/page-stub";

export default function Page() {
  return (
    <PageStub
      title="Requests"
      note="Business Trip, Expense, and Leave requests with approval workflow, calendar & notifications. Design in docs/REQUESTS-MODULE.md — built after foundation approval."
    />
  );
}
