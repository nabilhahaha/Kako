import { PageStub } from "@/components/app/page-stub";
export default function Page() {
  return <PageStub title="Raw Data Upload" note="Select agent, upload Excel/CSV, detect sheet/headers/date range — screen #4." />;
}
